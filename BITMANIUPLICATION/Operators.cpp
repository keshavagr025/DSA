#include <iostream>

using namespace std;

int main(){

    cout << "The bitwise AND & = " << (5 & 5) << endl;
    cout << "The bitwise OR | = " << (5 | 5) << endl;
    cout << "The bitwise XOR ^ = " << (5 ^ 5) << endl;
    cout << "The bitwise NOT ~ = " << (~4) << endl;
}